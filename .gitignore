# dependencies
node_modules/

# secrets — never commit (documentation §13)
.env
.env.local
.env.*.local
!.env.example

# signing keys — the platform Ed25519 private key must never enter the repo (§10.1, Sprint 4 Day 16)
*.pem
keys/

# builds
dist/
build/
.next/
out/
*.tsbuildinfo

# prisma generated client
apps/api/src/generated/

# test + coverage
coverage/
.jest-cache/

# logs
*.log
npm-debug.log*

# editors / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# browser smoke-test output
screenshots/

# contract workspace uploads (Sprint 5)
uploads/

# e2e fixtures generated at run time
e2e/.fixture-*

# AI assistant scaffolding (regenerated by tooling; not part of the app)
CLAUDE.md
AGENTS.md
.claude/
.cursor/
.github/copilot-instructions.md
